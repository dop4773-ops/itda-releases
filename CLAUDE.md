# 잇다 (itda) 프로젝트 작업 규칙

이 파일은 클로드코드가 세션 시작 시 자동으로 읽는 규칙 파일입니다. 아래 내용은 특별히 다른 지시가 없는 한 모든 세션에 기본 적용됩니다.

## 프로젝트 개요
- Electron 기반 개인 업무관리 앱 (바닐라JS, TypeScript 아님)
- Todo/캘린더(Google Calendar 연동)/메모/포스트잇/검색(FTS5)/연결(link) 시스템/보안 잠금/자동 업데이트 등 핵심 기능 보유
- 1인 개발 + 클로드코드 페어 프로그래밍으로 진행 중

## 버그 수정 작업 시 반드시 지킬 4단계

버그 관련 작업을 지시받으면 "고쳤습니다"로 끝내지 말고 아래 순서를 반드시 거칠 것:

1. **재현** — 코드만 보고 추측하지 말고 CDP 또는 실기 실행으로 실제 증상을 먼저 재현해서 확인한다.
2. **원인 진단 보고** — 코드를 고치기 전에 원인이 무엇인지 먼저 설명한다. 원인을 모른 채 증상만 없애는 수정(예: 에러를 try/catch로 감싸서 조용히 무시하는 식)은 금지.
3. **수정** — 원인에 맞는 수정을 한다.
4. **회귀 확인** — 고친 부분과 인접한 기능까지 같이 실제로 확인하고, 결과를 보고한다.

## 패치 vs 재설계 구분

- 같은 영역에서 버그가 이미 2번 이상 재발한 이력이 있다면 (KNOWN_ISSUES.md 참고), 기본적으로 최소 변경(패치)이 아니라 **왜 이 부분이 구조적으로 깨지기 쉬운지부터 분석**하고 필요하면 해당 부분을 재설계할 것.
- 사용자가 "재설계" 또는 "구조적으로 봐달라"고 명시하지 않은 일반 버그는 최소 변경 패치로 처리해도 됨. 단, 그 경우에도 4단계(재현→진단→수정→회귀확인)는 동일하게 적용.

## 작업 우선순위 티어 (한 세션에는 한 티어만)

- **1티어 (즉시)**: 크래시, 데이터 손실 위험, 보안 기능(잠금) 오작동
- **2티어**: 핵심 플로우 부분 장애 (캘린더 동기화, 검색, 연결 기능 등)
- **3티어**: 우회 가능한 UI/UX 불편 사항

사용자가 티어를 명시하지 않고 여러 문제를 한 번에 던지면, 가장 높은 티어부터 처리하고 나머지는 이번 세션 범위 밖임을 먼저 알릴 것.

## Electron 관련 주의사항

- `prompt()`, `alert()`, `confirm()` 등 브라우저 전용 API는 Electron 렌더러에서 제대로 동작하지 않을 수 있음 — 절대 사용하지 말 것. 다이얼로그가 필요하면 기존 커스텀 모달 컴포넌트를 재사용할 것.
- 새 코드 작성 시 `npm run lint` 를 통과하는지 확인할 것 (에러 0개 기준).

## 세션 종료 전 체크

- 이번 세션에서 손댄 기능과 관련된 항목을 `RELEASE_CHECKLIST.md`에서 찾아 실제로 재검증하고, 결과(통과/실패)를 보고할 것.
- 이번 세션에서 새로 발견했지만 이번 범위에서 고치지 않은 이슈는 `KNOWN_ISSUES.md`에 추가할 것 (심각도, 재현 조건, 시도한 것 포함).
- 기존에 `KNOWN_ISSUES.md`에 있던 이슈를 이번에 해결했다면 해당 항목을 "해결됨"으로 갱신하고 해결 방법을 남길 것 (삭제하지 말 것 — 나중에 재발 시 이력 참고용).

## 하지 말 것

- 지시받지 않은 새 기능 추가 (기능 추가는 별도 세션에서 명시적으로 요청됨)
- TypeScript 전환, 대규모 아키텍처 리팩터링을 임의로 진행 (제안은 가능하나 사용자 승인 없이 실행하지 말 것)
- 원인 파악 없이 증상만 가리는 수정
