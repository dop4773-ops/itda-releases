# 잇다 스모크 테스트 체크리스트 (Windows 실기 검증용)

jsdom으로 로직은 이미 검증되어 있습니다. 여기서는 **실제 Electron 창에서만
확인 가능한 것들**(렌더링, 폰트, 네이티브 UI, 창 동작)을 체크합니다.
문제가 생기면 항목 번호와 증상을 메모해두면 나중에 원인 찾기 쉬워요.

## 0. 설치 전
- [ ] `scripts\check_windows_env.ps1` 실행 → 문제 없음 확인
- [ ] `npm install` 실행 → 에러 없이 완료 (특히 `better-sqlite3` 빌드/설치 단계)
- [ ] `npm start` 실행 → 창이 뜨는지 확인 (몇 초 정도 걸릴 수 있음)

## 1. 창/셸
- [ ] 창 크기 조절, 최소화/복원이 정상 동작
- [ ] 사이드바 접기 버튼 클릭 → 아이콘만 남고 텍스트 사라짐
- [ ] 앱을 껐다 다시 켰을 때 접힘 상태가 유지됨 (SQLite 저장 확인)
- [ ] `Ctrl+K` (Mac이 아니므로 `⌘K` 대신) → 빠른입력 모달 열림
- [ ] 모달에 텍스트 입력 후 Enter → Inbox에 저장되고 토스트 표시
- [ ] 한글 입력(한글 IME) 정상 동작 확인 — 조합 중 글자가 깨지지 않는지

## 2. 대시보드
- [ ] 첫 실행 시 모든 카드가 "0건" 또는 빈 상태로 정상 표시 (에러 없이)
- [ ] 요약카드 색상 4개 다르게 표시됨
- [ ] 하단 Inbox 바에서 입력 → 추가 버튼 클릭 → 저장됨

## 3. Inbox
- [ ] 항목 추가 → 미처리 탭에 표시
- [ ] "Todo로 전환" 클릭 → Todo 화면에 실제로 생성됐는지 확인
- [ ] 처리됨 탭에서 전환 이력 확인

## 4. Todo
- [ ] 카테고리 드롭다운에 기본 4개(회의·업무/상담/교육/외래) 표시
- [ ] 날짜 선택(`<input type="date">`)이 Windows에서 정상 동작 (달력 피커 뜨는지)
- [ ] 우선순위 "높음" + 어제 날짜로 추가 → "지남" 배지가 빨간색으로 표시
- [ ] 체크박스 클릭 → 완료 탭으로 이동, 취소선 표시

## 5. 일정 (그리드뷰) — 가장 중점적으로 확인할 부분
- [ ] 월간 뷰: 6주 그리드가 화면에 다 들어오는지 (해상도 낮은 병원 PC 특성상 스크롤/잘림 확인)
- [ ] 이전/다음 버튼으로 달 이동, "오늘" 버튼으로 복귀
- [ ] `<input type="datetime-local">` 모달에서 정상 동작 (Windows 네이티브 피커)
- [ ] 일정 추가 → 해당 날짜 셀에 색깔 pill로 표시
- [ ] 날짜 셀 클릭 → 일간 뷰로 전환되는지
- [ ] 일간/주간 뷰: 시간대에 맞게 일정 블록이 정확한 위치에 표시되는지 (예: 09:00 일정이 그리드 상단 근처에 오는지)
- [ ] 일정 블록에 마우스 올리면 × 삭제 버튼 나타나는지 (터치스크린 PC라면 호버가 안 될 수 있음 — 해당되면 메모)

## 6. 메모 / 포스트잇
- [ ] 카드가 메이슨리(계단식) 레이아웃으로 배치되는지, 살짝 기울어져 보이는지
- [ ] 카드 호버 시 정렬되며 그림자 커지는지
- [ ] 색상 스와치 클릭 → 즉시 배경색 변경
- [ ] 제목/내용 입력 후 잠깐 기다리면 자동 저장(별도 저장 버튼 없음) — 껐다 켜서 남아있는지 확인

## 7. 검색
- [ ] 검색어 입력 → 결과가 Todo/일정/메모/포스트잇별로 그룹핑되어 표시
- [ ] 결과 클릭 → 해당 화면으로 이동하는지

## 8. 휴지통 / 설정
- [ ] 삭제한 항목이 휴지통에 타입 아이콘과 함께 표시
- [ ] 복원 클릭 → 원래 화면에 다시 나타나는지
- [ ] 설정에서 카테고리 색상 변경(네이티브 컬러피커) → 캘린더/Todo에 바로 반영되는지
- [ ] 기본 카테고리 4개는 삭제 버튼이 없고 "기본" 배지만 있는지

## 9. 재시작 내구성
- [ ] 여러 화면에 데이터를 넣은 상태에서 앱을 완전히 종료 후 재실행 → 데이터 전부 유지되는지
- [ ] `%APPDATA%\잇다\assistant.db` 파일이 실제로 생성되어 있는지 확인

## 문제가 생기면
- 콘솔 로그가 필요하면 `main/main.js`의 `// mainWindow.webContents.openDevTools();` 주석을 해제하고 다시 `npm start`
- DB 자체를 초기화하고 싶으면 `%APPDATA%\잇다\assistant.db*` 파일들을 지우고 재실행 (스키마가 자동 재생성됨)
