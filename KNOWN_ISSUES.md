# 잇다 (itda) 알려진 이슈

세션 시작 시 이 파일을 먼저 읽고, 관련 작업이면 아래 이력을 참고할 것. 새로 발견된 이슈는 아래 템플릿으로 추가하고, 해결된 이슈는 삭제하지 말고 "해결됨"으로 상태만 갱신할 것 (재발 시 이력 추적용).

---

## [미해결] 캘린더 드래그아웃 한계

- **심각도**: 2티어 (핵심 플로우 부분 장애 — 일정 이동/편집 시 불편)
- **재현 조건**: 캘린더에서 일정을 드래그하여 화면 밖 영역으로 옮기려 할 때
- **이력**:
  - 8/12 세션: 구조적 원인이 규명됨, "근본 한계"로 판단되어 Windows 실기 재테스트로 보류
  - 이후 명확한 후속 해결 기록 없음 — 현재 상태 불명확, 재현부터 다시 확인 필요
- **다음 시도 시 참고**: 이전에 "구조적 한계"로 결론났던 이유가 무엇이었는지 먼저 코드/이전 세션 기록에서 확인하고, 같은 접근을 반복하지 말 것. 패치보다 재설계 검토 대상.

---

## [미해결] Google Calendar OAuth 토큰 갱신 문제

- **심각도**: 2티어 (동기화 기능 장애로 이어짐)
- **재현 조건**: 일정 기간(약 7일) 앱을 사용하지 않거나 refresh token이 만료되는 시점
- **이력**:
  - 8/8 세션: "7일 refresh token 만료 문제" 최초 확인
  - 8/18 세션: "Google Calendar 인증 경로 버그 수정" — 그러나 근본 원인(토큰 갱신 로직)이 해결됐는지는 불명확
- **다음 시도 시 참고**: 두 번 재발했으므로 이번엔 패치보다 OAuth 토큰 갱신 흐름 전체(토큰 저장 위치, 만료 감지, 자동 재갱신 로직)를 구조적으로 리뷰할 것. 8/18 수정이 실제로 무엇을 고쳤는지부터 코드 확인 필요.

---

## [상태 확인 필요] 캘린더 클릭 관련 버그

- **심각도**: 확인 후 재분류 필요
- **재현 조건**: 캘린더 종일 일정/클릭 상호작용 (정확한 재현 조건은 재확인 필요)
- **이력**:
  - 8/12 세션 내에서 세 번 손봄: 부분수정(미해결 남음) → 구조적 수정(3블록 분리) → 실사용 확인
  - 마지막 기록상으로는 "확인 완료"로 남아 있으나, 이후 세션에서 관련 버그가 재언급되지 않아 실제로 안정화됐는지 별도 확인된 적 없음
- **다음 시도 시 참고**: 새 버그 취급하지 말고, 먼저 현재 상태가 실제로 정상인지부터 재검증할 것.

---

## [해결됨] 설정 > 보안 잠금 끄기 다이얼로그 미동작 + prompt/alert/confirm 전수 점검

- **심각도**: 1티어 (보안 기능 오작동)였음
- **재현 조건**: 설정 > 보안에서 잠금 끄기 시도 시 `prompt()` 다이얼로그가 Electron 렌더러에서 지원되지 않아 아무 반응 없음
- **이력**:
  - 8/28 세션(v2.34.1): 잠금 끄기의 `prompt()` → 커스텀 `promptText()` 팝오버로 수정 완료
  - 8/30 세션(v2.58.1): 코드베이스 전수 점검 완료. `prompt()`/`alert()`는 없었고, `confirm()` 2곳(메모 일괄삭제)만 있어서 `renderer/shared/confirm-dialog.js`의 `confirmDialog()`로 교체.
- **재발 방지**: ESLint `no-restricted-globals`로 렌더러에서 `prompt`/`confirm`/`alert` 사용을 **에러**로 막음. `.github/workflows/release.yml`에 `npm run lint` 게이트가 있어 에러가 있으면 릴리스 자체가 안 올라감.

---

## [해결됨] 연결된 항목 내용 동기화 — auto 모드에서 서식/체크박스가 평문으로 손실

- **심각도**: 2티어 (연결 기능 관련 데이터 손실)였음
- **재현 조건 (수정 전)**:
  1. 설정 > 편의 기능 > "연결된 항목 내용 동기화"를 **auto("자동으로 맞추기")** 로 설정
  2. 체크박스·볼드·글자색·인라인 사진이 들어간 포스트잇/메모를 Todo(또는 일정)와 연결
  3. 연결된 Todo/일정 쪽의 제목·메모를 수정
  4. → 1.2초 뒤 포스트잇/메모의 본문이 **평문으로 덮어써지며 체크박스/서식/사진이 사라짐** (확인창 없이 조용히)
- **이력**:
  - 8/29 세션(v2.54.0): 기능 최초 구현. 원 스펙이 "제목·본문 **텍스트**만 동기화, 타입 고유 필드 제외"라, `main/link-sync.js`가 HTML을 `htmlToPlain`으로 벗겨 비교/전파하고 되쓸 때 `plainToHtml`(줄바꿈만 `<br>`)로 되돌림 → 서식은 의도적으로 전파 안 함.
  - 8/30 세션: 전체 시나리오 CDP 재검증 — off/auto/ask 전 방향, 무한루프 없음, 타입 고유 필드 보존, 위젯 창 편집 동기화, 디바운스 정상, 에러 0건. **동작 자체는 정상**이고 서식 손실만 남음 확인.
- **해결 (8/30 세션, v2.58.2)**:
  - `main/link-sync.js`에 `RICH_MARKERS` 정규식 + `wouldLoseFormatting()` 추가. **auto 모드라도** 동기화 대상 memo/postit의 현재 본문에 서식 마커(`<input>` 체크박스 / `<img>` / `<b><i><u>` 등 / `style·class` 붙은 `<span>` / `text-align`)가 있고 본문 텍스트가 실제로 바뀌는 경우엔, 덮어쓰기 전에 확인창을 띄운다: `"연결된 [항목]에 체크박스·서식이 있어요. 내용을 맞추면 그 서식이 사라집니다. 그래도 바꿀까요?"` (기본 선택 = **아니오**, "다시 묻지 않기" 없음 = 매번 물음).
  - 서식 없는 항목 / Todo·일정(평문 memo 칸) 대상은 종전대로 auto에서 조용히 반영.
  - 스텁 다이얼로그 분기 테스트 + 라이브 CDP 검증 완료.
- **남은 한계 (설계상, 이슈 아님)**: Todo/일정이 낀 연결에서 [예]를 누르면 여전히 서식은 사라진다(memo 칸이 평문 전용이라). 이제는 조용히가 아니라 **동의를 받고** 사라지는 것. memo↔postit끼리 서식까지 보존 전파하려면 `sanitizeRichHtml` 경유 별도 작업 필요.

---

## 새 이슈 추가 템플릿

```
## [미해결] 이슈 제목

- **심각도**: 1티어 / 2티어 / 3티어
- **재현 조건**:
- **이력**:
  - 날짜 세션: 무엇을 했고 결과가 어땠는지
- **다음 시도 시 참고**: 이전에 시도했다가 안 됐던 방법, 구조적 원인 추정 등
```
