# 잇다 (itda) 알려진 이슈

세션 시작 시 이 파일을 먼저 읽고, 관련 작업이면 아래 이력을 참고할 것. 새로 발견된 이슈는 아래 템플릿으로 추가하고, 해결된 이슈는 삭제하지 말고 "해결됨"으로 상태만 갱신할 것 (재발 시 이력 추적용).

---

## [해결됨] 대시보드 위젯 가로선 어긋남 (윈도우) — 5개 버전 헛짚음 후 근본원인 확인

- **심각도**: 2~3티어 (레이아웃 어긋남, 기능 정상)
- **재현 조건**: 대시보드에서 2행 스팬 위젯(업무센터 등) 옆에 1행짜리 위젯을 놓으면, 1행 위젯의
  아래 모서리가 2행 위젯보다 12px 아래로 삐져나옴. 1행 위젯끼리는 아래 행과 간격이 14→2px로 좁음.
- **근본원인 (8/31 세션, v2.58.13)**: `.panel { min-height: 120px }` vs `.dash-widget-grid
  { grid-auto-rows: 108px }` 불일치. 고정 트랙(108)보다 min-height(120)가 커서 1행 위젯이
  트랙을 12px 넘침. mac은 덜 티나서 계속 못 잡음.
- **헛짚은 이력 (전부 링크 위젯 전용으로만 봄 — 실제론 전 위젯 공통)**:
  - v2.58.9: `.link-block`에 `scrollbar-gutter:stable`
  - v2.58.10: 스크롤바 숨김 + `box-shadow` 제거
  - v2.58.11: `align-self:stretch` + `flex:1`
  - v2.58.12: 프레임 블록 래퍼 `overflow:hidden`
  - → 사용자가 "다른 위젯도 어긋남 / 배율 100%"라고 알려줘서 그리드 행 문제로 방향 전환.
- **해결**: `grid-auto-rows: 120px`로 min-height와 통일. CDP로 2행 위젯 bottom == 옆 1행 위젯
  bottom, 행 간격 전부 14px 확인. 부작용: 모든 위젯 행당 12px 커짐(대시보드가 약간 길어짐).
- **다음 시도 시 참고**: "옆 위젯과 가로/세로선 안 맞음" 류는 개별 위젯 CSS 말고 **그리드
  트랙 크기 vs 아이템 min-height/min-width** 부터 볼 것. mac에서 재현 안 되면 실측 수치
  (getBoundingClientRect bottom 비교)로 12px 같은 상수 오차를 찾을 것.

---

## [부분해결] 화면 언마운트 후 debounce load()가 발동해 null 참조

- **심각도**: 3티어 (콘솔/error.log 에러 1건, 데이터 손실·크래시 없음)
- **재현 조건**: 어떤 화면에서 데이터 변경(추가/삭제 등)이 일어난 직후 200ms 안에 다른 화면으로 이동. 그 화면의 `onDataChanged` → `debounce(load, 200)` 타이머가 언마운트 후 발동 → `load()`가 `$('...')`로 컨테이너를 다시 찾는데 `root.innerHTML=''`로 이미 비워져서 null → `TypeError: Cannot read properties of null`.
- **이력**:
  - 8/31 세션: 로컬 경로 링크 작업 중 빠른 화면 전환 테스트에서 `postit.js:100` (`grid.classList.remove`)로 재현. `postit.js`에 `if (!grid) return;` 가드 추가(v2.58.5). `calendar.js`는 이미 `if (unmounted || !$('c-periodLabel')) return;` 가드가 있었음.
- **다음 시도 시 참고**: 같은 `debounce(load,200)` + `onDataChanged` 패턴이 `memo.js`(1138) / `inbox.js`(43) / `tags.js`(109)에도 있음 — 아직 가드 없음. 근본 해결은 `ui-utils.js`의 `debounce`가 `.cancel()`을 반환하게 하고 각 뷰 cleanup에서 취소하는 것. 아니면 각 `load()` 첫 줄에 컨테이너 null 가드. 전용 스윕 세션에서 4곳 한 번에.

---

## [감안한 한계] 확장자 없는 폴더 경로 뒤 같은 줄 문장이 링크에 딸려옴

- **심각도**: 3티어 (링크 텍스트만 길어짐, 클릭 시 열기는 실패할 수 있음)
- **배경**: 8/31 세션(v2.58.6). 메모/포스트잇 본문의 로컬 경로 자동 링크가 `C:\...\바탕 화면\...`처럼 공백 포함 윈도우 경로를 인식하도록 `rich-text.js`의 `URL_PATTERN`을 " (?=\S)"(뒤에 글자 오는 공백 1칸 허용)로 넓힘. UNC(`\\서버\공유 폴더\...`)·`(x86)`/`(OT)` 균형 괄호 복원 포함. 파일 경로(`.확장자`)는 조사/문장 앞에서 정확히 끊기지만, **확장자 없는 폴더 경로** 뒤에 구두점 없이 같은 줄로 문장이 이어지면 그 일부가 링크에 포함될 수 있음.
- **다음 시도 시 참고**: 공백 포함 한글 폴더명과 뒤따르는 한글 문장을 구분할 확실한 방법이 없어서 감안함. 사용자가 경로를 줄 단독/줄 끝에 두면 정확히 끊김. 정 필요하면 "공백 뒤 토큰에 경로 구분자(`\`/`/`)가 더 나오거나 그게 마지막 토큰일 때만 공백 허용" 같은 휴리스틱 검토.

---

## [미해결] 캘린더 드래그아웃 한계

- **심각도**: 2티어 (핵심 플로우 부분 장애 — 일정 이동/편집 시 불편)
- **재현 조건**: 캘린더에서 일정을 드래그하여 화면 밖 영역으로 옮기려 할 때
- **이력**:
  - 8/12 세션: 구조적 원인이 규명됨, "근본 한계"로 판단되어 Windows 실기 재테스트로 보류
  - 이후 명확한 후속 해결 기록 없음 — 현재 상태 불명확, 재현부터 다시 확인 필요
- **다음 시도 시 참고**: 이전에 "구조적 한계"로 결론났던 이유가 무엇이었는지 먼저 코드/이전 세션 기록에서 확인하고, 같은 접근을 반복하지 말 것. 패치보다 재설계 검토 대상.

---

## [미해결] Google Calendar OAuth 토큰 갱신 문제

- **심각도**: 2티어 (동기화 기능 장애로 이어짐)
- **재현 조건**: 일정 기간(약 7일) 앱을 사용하지 않거나 refresh token이 만료되는 시점
- **이력**:
  - 8/8 세션: "7일 refresh token 만료 문제" 최초 확인
  - 8/18 세션: "Google Calendar 인증 경로 버그 수정" — 그러나 근본 원인(토큰 갱신 로직)이 해결됐는지는 불명확
- **다음 시도 시 참고**: 두 번 재발했으므로 이번엔 패치보다 OAuth 토큰 갱신 흐름 전체(토큰 저장 위치, 만료 감지, 자동 재갱신 로직)를 구조적으로 리뷰할 것. 8/18 수정이 실제로 무엇을 고쳤는지부터 코드 확인 필요.

---

## [상태 확인 필요] 캘린더 클릭 관련 버그

- **심각도**: 확인 후 재분류 필요
- **재현 조건**: 캘린더 종일 일정/클릭 상호작용 (정확한 재현 조건은 재확인 필요)
- **이력**:
  - 8/12 세션 내에서 세 번 손봄: 부분수정(미해결 남음) → 구조적 수정(3블록 분리) → 실사용 확인
  - 마지막 기록상으로는 "확인 완료"로 남아 있으나, 이후 세션에서 관련 버그가 재언급되지 않아 실제로 안정화됐는지 별도 확인된 적 없음
- **다음 시도 시 참고**: 새 버그 취급하지 말고, 먼저 현재 상태가 실제로 정상인지부터 재검증할 것.

---

## [해결됨] 설정 > 보안 잠금 끄기 다이얼로그 미동작 + prompt/alert/confirm 전수 점검

- **심각도**: 1티어 (보안 기능 오작동)였음
- **재현 조건**: 설정 > 보안에서 잠금 끄기 시도 시 `prompt()` 다이얼로그가 Electron 렌더러에서 지원되지 않아 아무 반응 없음
- **이력**:
  - 8/28 세션(v2.34.1): 잠금 끄기의 `prompt()` → 커스텀 `promptText()` 팝오버로 수정 완료
  - 8/30 세션(v2.58.1): 코드베이스 전수 점검 완료. `prompt()`/`alert()`는 없었고, `confirm()` 2곳(메모 일괄삭제)만 있어서 `renderer/shared/confirm-dialog.js`의 `confirmDialog()`로 교체.
- **재발 방지**: ESLint `no-restricted-globals`로 렌더러에서 `prompt`/`confirm`/`alert` 사용을 **에러**로 막음. `.github/workflows/release.yml`에 `npm run lint` 게이트가 있어 에러가 있으면 릴리스 자체가 안 올라감.

---

## [해결됨] 연결된 항목 내용 동기화 — auto 모드에서 서식/체크박스가 평문으로 손실

- **심각도**: 2티어 (연결 기능 관련 데이터 손실)였음
- **재현 조건 (수정 전)**:
  1. 설정 > 편의 기능 > "연결된 항목 내용 동기화"를 **auto("자동으로 맞추기")** 로 설정
  2. 체크박스·볼드·글자색·인라인 사진이 들어간 포스트잇/메모를 Todo(또는 일정)와 연결
  3. 연결된 Todo/일정 쪽의 제목·메모를 수정
  4. → 1.2초 뒤 포스트잇/메모의 본문이 **평문으로 덮어써지며 체크박스/서식/사진이 사라짐** (확인창 없이 조용히)
- **이력**:
  - 8/29 세션(v2.54.0): 기능 최초 구현. 원 스펙이 "제목·본문 **텍스트**만 동기화, 타입 고유 필드 제외"라, `main/link-sync.js`가 HTML을 `htmlToPlain`으로 벗겨 비교/전파하고 되쓸 때 `plainToHtml`(줄바꿈만 `<br>`)로 되돌림 → 서식은 의도적으로 전파 안 함.
  - 8/30 세션: 전체 시나리오 CDP 재검증 — off/auto/ask 전 방향, 무한루프 없음, 타입 고유 필드 보존, 위젯 창 편집 동기화, 디바운스 정상, 에러 0건. **동작 자체는 정상**이고 서식 손실만 남음 확인.
- **1차 해결 (8/30 세션, v2.58.2)**:
  - `main/link-sync.js`에 `RICH_MARKERS` 정규식 + `wouldLoseFormatting()` 추가. **auto 모드라도** 동기화 대상 memo/postit의 현재 본문에 서식 마커(`<input>` 체크박스 / `<img>` / `<b><i><u>` 등 / `style·class` 붙은 `<span>` / `text-align`)가 있고 본문 텍스트가 실제로 바뀌는 경우엔, 덮어쓰기 전에 확인창을 띄운다.
- **2차 해결 (8/30 세션, v2.58.3) — memo ↔ postit는 서식 보존 전파**:
  - `readContent`에 `html` 필드 추가(memo/postit=저장된 서식 HTML, todo/event=null).
  - `applyContent`: 소스도 memo/postit(`src.html != null`)이면 저장된 서식 HTML을 **그대로 복사** — 체크박스(체크상태 포함)·볼드·글자색·인라인 사진까지 보존. 그 HTML은 renderer가 저장 시 이미 `sanitizeRichHtml`을 거쳤고 불러올 때 또 정화하므로 안전.
  - `needsUpdate`에 `richDiff` 추가 — memo↔postit는 텍스트가 같아도 서식 HTML이 다르면 동기화(볼드만 추가한 편집도 전파). 한쪽이 평문 타입이면 richDiff 무시.
  - `wouldLoseFormatting`: 소스가 memo/postit면 `false`(서식 보존되므로 경고 불필요). **Todo/일정이 낀 연결에서만** 서식 손실 경고가 뜬다.
  - 라이브 CDP 검증: memo(볼드+색+체크박스)↔postit 양방향 전파, 실제 에디터로 편집 시 두 에디터의 sanitize 결과가 동일 → **핑퐁/무한루프 없음**(memo↔postit 왕복 안정), 렌더링 정상. off/ask/todo↔postit 경고 회귀 정상.
- **남은 한계 (설계상, 이슈 아님)**: Todo/일정이 낀 연결은 memo 칸이 평문 전용이라 여전히 텍스트만 오간다. 서식 있는 memo/postit를 Todo/일정에 맞추면 서식이 빠지지만, 이제 auto에서도 **확인을 받고** 빠진다(조용히 아님).

---

## 새 이슈 추가 템플릿

```
## [미해결] 이슈 제목

- **심각도**: 1티어 / 2티어 / 3티어
- **재현 조건**:
- **이력**:
  - 날짜 세션: 무엇을 했고 결과가 어땠는지
- **다음 시도 시 참고**: 이전에 시도했다가 안 됐던 방법, 구조적 원인 추정 등
```
