# 릴리스 전 체크리스트

새 버전을 푸시하기 전에 아래 항목을 **하나씩 실제로** 확인한다.
(그때그때 눈대중으로 확인하다 배포 직전에 앱이 안 뜨는 상태까지 간 적이 있어서 만든 표준 절차)

검증은 사용자 실행 중인 앱을 건드리지 않도록 **격리 프로파일**로 한다:

```bash
node node_modules/electron/cli.js . --remote-debugging-port=9223 --user-data-dir=/tmp/itda-testprofile
# 끝나면: pkill -f "user-data-dir=/tmp/itda-testprofile" && rm -rf /tmp/itda-testprofile
```

## 자동 게이트 (CI에서도 강제됨)

- [ ] `npm run lint` — 에러 0개 (경고는 무방)
- [ ] 변경한 파일 전부 `node -c <file>` 문법 통과

## 기능 스모크 (격리 프로파일 CDP 또는 실기)

- [ ] 앱 실행 후 크래시 없이 대시보드가 로딩됨
- [ ] Todo: 생성 / 수정(제목·메모) / 완료 토글 / 삭제(휴지통) 정상
- [ ] 캘린더: 일정 생성 / 수정 / 삭제 정상, Google Calendar 연동 계정은 동기화 정상
- [ ] 메모: 생성 / 수정 / 삭제, 리치텍스트(체크박스 토글 · 글자색 · 링크 삽입 · 사진/파일 첨부) 정상
- [ ] 포스트잇: 생성 / 내용 수정(자동저장) / 색상 변경 / 삭제 정상
- [ ] 검색(FTS5): 한글·영문 키워드로 Todo/메모/포스트잇/일정이 검색됨
- [ ] 연결(link): 우클릭 → 연결 / 전환(Todo·일정·메모로 만들기) / 연결 해제 정상, 양쪽에서 즉시 반영
- [ ] 위젯 창(포스트잇/메모/할일/일정/보드류)에서도 우클릭 메뉴 동일하게 뜸
- [ ] **설정 > 보안**: 잠금 켜기 → 앱 재시작 → 잠금화면 진입 → 비밀번호로 해제 → 설정에서 비밀번호 변경 → 잠금 끄기(비밀번호 확인) 전체 흐름 정상 (네이티브 prompt 안 씀)
- [ ] 데이터: 백업 → 복원 왕복, 복원 후 데이터 유실 없음
- [ ] 다크모드 ↔ 라이트모드 전환 시 깨지는 화면 없음
- [ ] 설정 > 데이터 & 백업 > "오류 로그 폴더 열기" 버튼이 폴더를 엶

## 빌드 후

- [ ] GitHub Actions 워크플로 success (lint 게이트 포함)
- [ ] GitHub Releases 자산 확인: `itda-setup-<버전>.exe` + `latest.yml`의 `updated_at`/`releaseDate`가 방금 시각인지 (같은 버전 재커밋 시 조용히 유실되는 함정 방지)
- [ ] (가능하면) 빌드된 설치본을 실제로 실행해서 몇 초 이상 크래시 없이 살아있는지

## 통과 안 되는 항목 처리

- 이번 릴리스에서 새로 깨진 것 → **고치고** 다시 확인
- 원래부터 알려진 미해결 이슈(예: 캘린더 드래그아웃, Google OAuth 토큰 갱신) → 여기 고치지 말고 별도 세션 TODO로만 기록
