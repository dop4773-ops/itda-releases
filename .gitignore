# 빌드 산출물 - GitHub Actions가 태그 올릴 때마다 자동으로 만들어주므로 커밋 불필요
dist/
build/
Output/
*.spec

# 사용자 데이터 - 병원 실제 데이터/개인정보 포함될 수 있어서 절대 커밋하면 안 됨
assistant.db
itda_config.json
itda_debug.log
*.db

# 모델 파일 - 수백MB~1GB라 git에 안 어울림, 각자 로컬에 따로 받아서 설정에서 경로 지정
models/
*.gguf

# 파이썬 캐시
__pycache__/
*.pyc
.venv/
