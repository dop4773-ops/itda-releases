# 잇다(Itda) - GitHub로 업데이트 배포하는 방법

설정 화면의 "업데이트" 카드에 GitHub 저장소(owner/repo)를 입력하면, 그 저장소의
"최신 릴리스(Latest Release)" 버전과 현재 앱 버전을 비교해줍니다. 이 기능을 실제로
쓰려면 GitHub에 저장소를 만들고 "릴리스"를 등록해두어야 합니다. 아래 순서대로 하시면 됩니다.

---

## 1. GitHub 저장소 만들기 (한 번만)

1. https://github.com 에서 계정 로그인 (없으면 가입, 무료)
2. 우측 상단 "+" → "New repository" 클릭
3. 저장소 이름 예: `itda-releases` (프라이빗으로 만들어도 되지만, **프라이빗이면
   앱에서 업데이트 확인이 인증 없이는 안 돼요** - 간단하게 쓰려면 Public 추천)
4. "Create repository" 클릭

저장소 주소가 `https://github.com/내아이디/itda-releases` 라면,
설정 화면에는 `내아이디/itda-releases` 만 입력하면 됩니다.

## 2. 새 버전 릴리스 만들기 (버전 올릴 때마다)

1. 저장소 페이지에서 우측의 "Releases" → "Create a new release" 클릭
2. **"Tag"에 버전 번호를 `v1.2.0` 처럼 입력** (중요: 앞에 `v`를 붙이는 게 관례이고,
   앱이 자동으로 `v`를 떼고 비교하니 붙여도 안 붙여도 상관은 없어요. 단, 반드시
   `itda_review_app.py`의 `APP_VERSION` 값이랑 숫자가 일치해야 "최신 버전"으로 인식돼요)
3. "Release title" 아무거나 (예: "1.2.0 - 대시보드 캘린더 추가")
4. 설명란에 이번 버전에서 뭐가 바뀌었는지 적어두면 나중에 참고하기 좋아요
5. 하단 "Attach binaries" 부분에 **`Output\Itda_Setup.exe` 파일을 드래그해서 올려두세요**
   (이게 실제로 사람들이 다운받을 설치 파일이 돼요)
6. "Publish release" 클릭

## 3. 앱 코드의 버전 번호도 맞춰서 올리기

`itda_review_app.py` 파일에서:
```python
APP_VERSION = "1.1.0"
```
이 줄을 릴리스한 버전과 똑같이 맞춰서 빌드해야 해요. 예를 들어 GitHub에
`v1.2.0`으로 릴리스했으면, 이 줄도 `"1.2.0"`으로 바꾸고 `rebuild_all.ps1`로
다시 빌드해서 배포해야 앱이 스스로 "지금 내가 최신 버전이다"라고 인식해요.

`installer.iss`의 버전도 같이 맞춰주면 더 좋아요 (설치 프로그램 자체 버전 표시용):
```
#define MyAppVersion "1.2.0"
```

## 4. 실제 업데이트 흐름 (병원 PC에서 쓰는 사람 입장)

1. 잇다 앱 → 좌측 하단 프로필 클릭 → 설정
2. "업데이트" 카드에 저장소(`내아이디/itda-releases`) 입력하고 저장
3. "업데이트 확인" 클릭
4. **새 버전이 있으면** "새 버전이 있어요: v1.2.0 (현재 v1.1.0) - 다운로드 페이지 열기" 링크가 떠요
5. 그 링크 클릭하면 GitHub 릴리스 페이지가 브라우저로 열리고, 거기서
   `Itda_Setup.exe`를 받아서 실행하면 기존 앱 위에 자동으로 업그레이드 설치돼요
   (기존에 저장해둔 `assistant.db`, `itda_config.json`은 그대로 유지됩니다)

## 참고: 지금은 수동 확인, 나중에 자동화하려면

지금 만들어진 기능은 "확인" 버튼을 눌러야 체크하는 수동 방식이에요. 나중에 이걸
더 발전시키고 싶다면:
- 앱 시작할 때 자동으로 한 번 체크하게 (itda_app.py 시작 시 백그라운드로 확인)
- "새 버전이 있어요" 발견하면 자동으로 설치 파일을 다운받아서 실행하는 원클릭 업데이트

이런 자동화는 지금 범위 밖이라 다음에 필요하면 따로 요청해주세요.

## 병원 네트워크가 막혀있다면?

만약 사무실 PC가 GitHub(`api.github.com`)에 접근이 안 되는 폐쇄망이면, "업데이트
확인" 버튼을 눌러도 실패 메시지만 뜨고 정상입니다 (에러 메시지에 이유가 표시돼요).
이 경우엔 그냥 새 zip 파일을 USB 등으로 직접 전달하는 지금까지의 방식을 계속
쓰시면 됩니다 - 업데이트 확인 기능은 "있으면 편한" 선택 기능이지, 필수는 아니에요.

## Windows SmartScreen 경고가 뜬다면?

`Itda_Setup.exe`를 다운로드하거나 실행할 때 "Windows에서 PC를 보호했습니다"
(파란 방패 아이콘) 경고가 뜰 수 있어요. 이건 정상입니다 - 코드 서명 인증서가
없는 소규모 프로그램이라 아직 Microsoft SmartScreen에 "평판"이 없어서 뜨는
경고예요, 악성코드라서가 아니에요.

- **수동으로 설치할 때**: 경고가 뜨면 "추가 정보" 클릭 → "실행" 버튼을 누르면 돼요.
- **앱 안의 "지금 업데이트"로 자동 설치할 때**: 앱이 조용히 종료된 뒤 이 경고가
  뜰 수 있는데, 이때 클릭 안 하고 넘어가면 설치가 그 상태로 멈춰있는 것처럼
  보여요. 화면에 이 창이 떠있는지 (다른 창에 가려져 있을 수도 있어요) 확인하고
  "추가 정보" → "실행"을 눌러주세요.
- **완전히 없애려면**: 정식 코드 서명 인증서(연 10만원~수십만원, 사업자 인증 필요)를
  구매해서 exe에 서명해야 하는데, 병원 내부용 개인 도구 규모에서는 비용 대비
  효율이 낮아서 권장하지 않아요. 대신 다운로드/실행 횟수가 쌓이면 시간이 지나며
  자연스럽게 경고 빈도가 줄어들기도 해요.
